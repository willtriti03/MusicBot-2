#
# Use requirements.lock for reproducible deployments. The py-cord minimum below
# keeps manual installs on the DAVE-era voice stack.
#
pynacl
py-cord[voice]>=2.7.1
pip
yt-dlp>=2024.1.0
colorlog
cffi --only-binary all; sys_platform == 'win32'
certifi
configupdater
SpeechRecognition>=3.10.0
PyAudio>=0.2.11
python-dotenv>=1.0.0
